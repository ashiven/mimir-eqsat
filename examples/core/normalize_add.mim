plugin core;

con extern add0 [i :I8, return : Cn I8] =
    return (%core.wrap.add 0 (i, 0I8));

con extern add_lit [return : Cn I8] =
    return (%core.wrap.add 0 (6I8, 0I8));

con extern add_lit_nat [return : Cn Nat] =
    return (%core.nat.sub (6, 3));
