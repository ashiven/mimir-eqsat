(root
  extern
  main
  (lam
    $f22
    (scope
      (lit ff Bool)
      (let
        $f21
        (scope
          (extract (var $f22) (lit tt Bool))
          (let
            $f20
            (scope
              (extract (extract (var $f22) (lit ff Bool)) (lit 0 (idx (lit 3 Nat))))
              (app (var $f21) (tuple (cons (var $f20) (cons (lit 48 I32) nil)))))))))))
